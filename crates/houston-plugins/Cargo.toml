[package]
name = "houston-plugins"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "The WASM plugin host for Houston 2.0 — a fuel-metered sandbox over a tiny bytes-in/bytes-out ABI."

[dependencies]
houston-api = { path = "../houston-api" }
anyhow.workspace = true
serde_json = "1"
wasmtime = "27"
