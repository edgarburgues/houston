[package]
name = "houston-api"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "The versioned component contract Houston 2.0 plugins build against."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
